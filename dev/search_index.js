var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"Everything is reached through the module: this package exports nothing, so calls are written HelmholtzDecomposition.plan_helmholtz(...) or via an alias, HD.plan_helmholtz(...).","title":"API"},{"category":"section","location":"api/#Entry-points","page":"API","text":"","title":"Entry points"},{"category":"section","location":"api/#Plan,-workspace-and-results","page":"API","text":"","title":"Plan, workspace and results"},{"category":"section","location":"api/#Reading-a-result","page":"API","text":"The staggered fields are where the projection is exact; the collocated ones on the result are these interpolated back to cell centres.","title":"Reading a result"},{"category":"section","location":"api/#Boundary-conditions","page":"API","text":"There is no Periodic boundary condition: whether a direction wraps is a property of the grid's topology, and a direction that wraps has no boundary for a condition to act on.","title":"Boundary conditions"},{"category":"section","location":"api/#Solvers","page":"API","text":"","title":"Solvers"},{"category":"section","location":"api/#Capability","page":"API","text":"AutoSolver chooses on these, and a solver named directly is refused rather than allowed to solve a different problem.","title":"Capability"},{"category":"section","location":"api/#Operators","page":"API","text":"","title":"Operators"},{"category":"section","location":"api/#Staggering-and-the-dual-grid","page":"API","text":"","title":"Staggering and the dual grid"},{"category":"section","location":"api/#Execution","page":"API","text":"","title":"Execution"},{"category":"section","location":"api/#Multigrid","page":"API","text":"","title":"Multigrid"},{"category":"section","location":"api/#Spectral","page":"API","text":"","title":"Spectral"},{"category":"module","location":"api/#HelmholtzDecomposition.HelmholtzDecomposition","page":"API","text":"HelmholtzDecomposition.jl — Helmholtz–Hodge decomposition of velocity fields.\n\nDecomposes a velocity field into rotational (divergence-free), divergent (curl-free) and harmonic parts, in any number of dimensions, on Cartesian and spherical grids.\n\nWhy this package exists\n\nOn the sphere, filtering velocity Cartesian components does not commute with differential operators (Aluie 2019, Proposition 2). The correct approach — mathematically equivalent to the generalized convolution that does commute — is to filter the scalar Helmholtz potentials (ψ, χ) separately. This package provides the decomposition step.\n\nSolver Extensions (important for performance!)\n\nThe base package includes only the SOR iterative solver, which works on any grid but may be orders of magnitude slower than spectral solvers. Load an appropriate extension:\n\nGeometry Regular Grid Irregular Grid\nCartesian using FFTW using FINUFFT\nSpherical using FastSphericalHarmonics using NUFSHT\n\nQuick Start\n\nusing HelmholtzDecomposition: HelmholtzDecomposition as HD\nusing FlowGeometries: FlowGeometries as FG\nusing FFTW: FFTW  # load spectral extension for Cartesian grids\n\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), xs, ys)\nresult = HD.helmholtz_decompose(u, v, grid)\n# result.u_rot, result.v_rot  — rotational velocity\n# result.u_div, result.v_div  — divergent velocity\n# result.ψ, result.χ          — scalar potentials\n\nNothing is exported: every name is reached as HelmholtzDecomposition.name.\n\nReferences\n\nAluie (2019): doi:10.1007/s13137-019-0123-9 — Convolutions on the sphere\nGlötzl & Richters (2023): doi:10.1016/j.jmaa.2023.127138 — n-dimensional Helmholtz potentials\nBuzzicotti et al. (2023): doi:10.1126/sciadv.adi7420 — Global cascade of kinetic energy\nStorer et al. (2022): doi:10.1038/s41467-022-33031-3 — Global energy spectrum\n\n\n\n\n\n","title":"HelmholtzDecomposition.HelmholtzDecomposition"},{"category":"function","location":"api/#HelmholtzDecomposition.helmholtz_decompose","page":"API","text":"helmholtz_decompose(u, grid; boundary = Neumann(), solver = AutoSolver()) -> HelmholtzResult\n\nDecompose a cell-centred, component-last velocity field on grid.\n\nBuilds a HelmholtzPlan and discards it. A caller decomposing more than one field on the same grid should build the plan once and call helmholtz_decompose! — the plan holds the grid's Laplacian coefficients, which are the expensive part.\n\n\n\n\n\n","title":"HelmholtzDecomposition.helmholtz_decompose"},{"category":"function","location":"api/#HelmholtzDecomposition.helmholtz_decompose!","page":"API","text":"helmholtz_decompose!(result, u, plan, ws = allocate_workspace(plan)) -> result\n\nDecompose the cell-centred, component-last velocity u into result, in place.\n\nThe solver is not an argument here: it is fixed by plan. Accepting one would either be ignored — a silent no-op — or force the plan's transform state to be rebuilt for a different solver, which is the per-call cost the plan exists to remove. Pass solver to plan_helmholtz.\n\n\n\n\n\n","title":"HelmholtzDecomposition.helmholtz_decompose!"},{"category":"function","location":"api/#HelmholtzDecomposition.helmholtz_decompose_batch","page":"API","text":"helmholtz_decompose_batch(plan, fields; backend = AutoBackend())\n\nDecompose many fields sharing one grid. The fields are independent, so the batch is the parallel axis: ThreadedBackend (OhMyThreads ext), DistributedBackend (Distributed ext) and MPIBackend (MPI ext) each spread it across their workers; a serial backend maps sequentially. Results come back in input order.\n\nOne plan serves the whole batch — its Laplacian coefficients are identical for every field and are the expensive part — while each task takes its own HelmholtzWorkspace, since those buffers are written through.\n\nbackend here is the batch axis. A parallel one pins the decomposition inside each field to serial; a serial one leaves the inner loops on the plan's own backend, so a short batch of large fields still parallelises — just along the other axis.\n\n\n\n\n\n","title":"HelmholtzDecomposition.helmholtz_decompose_batch"},{"category":"function","location":"api/#HelmholtzDecomposition.helmholtz_decompose_batch!","page":"API","text":"helmholtz_decompose_batch!(batch, fields, plan; backend = AutoBackend()) -> batch\n\nDecompose into a preallocated HelmholtzBatch.\n\n\n\n\n\n","title":"HelmholtzDecomposition.helmholtz_decompose_batch!"},{"category":"function","location":"api/#HelmholtzDecomposition.plan_helmholtz","page":"API","text":"plan_helmholtz(grid; boundary = Neumann(), solver = AutoSolver(), backend = AutoBackend())\n\nResolve everything a decomposition on grid fixes once: the Laplacian coefficients on the primal and dual grids, which solver will run, that solver's reusable state, and which execution backend the loops inside one decomposition use.\n\nThe solver is chosen here rather than per call deliberately. Choosing it reads the mask, which is O(N), and preparing it builds the transform plans; doing both inside helmholtz_decompose! repeats them for each of the P + 1 potentials and again for every field of a batch, which is exactly the work a batch exists to amortize.\n\nbackend is the intra-field one — it drives the operator and solver loops, so a single large field is parallel rather than only a batch of them. A batch overrides it with a serial inner backend, because an outer and an inner loop each claiming every thread is slower than either.\n\n\n\n\n\n","title":"HelmholtzDecomposition.plan_helmholtz"},{"category":"type","location":"api/#HelmholtzDecomposition.HelmholtzPlan","page":"API","text":"HelmholtzPlan\n\nThe shareable part of a decomposition on one (grid, boundary, eltype): the Laplacian coefficients on the primal grid and on each dual grid, and the dual grids themselves. Buffers live in a HelmholtzWorkspace, one per task, precisely so that a whole batch can share a single plan without racing.\n\nThe geometry behind those coefficients — face areas need the scale factors at the face, which on a curved grid is trigonometry per cell per direction — is invariant across solver iterations, across the P + 1 potentials, and across a whole batch. Rebuilding it per call is the single largest avoidable cost in a decomposition, so it is built here and only read afterwards.\n\n\n\n\n\n","title":"HelmholtzDecomposition.HelmholtzPlan"},{"category":"type","location":"api/#HelmholtzDecomposition.HelmholtzWorkspace","page":"API","text":"HelmholtzWorkspace\n\nThe mutable buffers one decomposition writes through, held apart from the plan.\n\nThe split is what makes a batch correct rather than merely possible. A HelmholtzPlan holds only things that are the same for every field on a grid — the Laplacian coefficients on the primal and dual grids — so a batch shares exactly one, which is the whole point, those being the expensive part. A workspace holds the face and corner buffers, which every field writes through, so each task needs its own; sharing one across threads would race on them.\n\n\n\n\n\n","title":"HelmholtzDecomposition.HelmholtzWorkspace"},{"category":"function","location":"api/#HelmholtzDecomposition.allocate_workspace","page":"API","text":"allocate_workspace(plan) -> HelmholtzWorkspace\n\nBuffers for one task working through plan.\n\n\n\n\n\n","title":"HelmholtzDecomposition.allocate_workspace"},{"category":"type","location":"api/#HelmholtzDecomposition.HelmholtzResult","page":"API","text":"HelmholtzResult\n\nVelocity-like fields use the component-last layout (dims..., N); potentials and scalar diagnostics are (dims...).\n\nThe array fields are const and the diagnostics are not: an in-place decomposition writes its convergence information into the result it was handed rather than allocating a fresh struct to carry two numbers.\n\n\n\n\n\n","title":"HelmholtzDecomposition.HelmholtzResult"},{"category":"function","location":"api/#HelmholtzDecomposition.allocate_result","page":"API","text":"allocate_result(plan) -> HelmholtzResult\n\nA zeroed result matching plan. Array types follow the plan's buffers, so a device-resident plan gives a device-resident result — nothing here hardcodes Array.\n\n\n\n\n\n","title":"HelmholtzDecomposition.allocate_result"},{"category":"type","location":"api/#HelmholtzDecomposition.HelmholtzBatch","page":"API","text":"HelmholtzBatch\n\nB decompositions on one grid, stored contiguously: each field of a HelmholtzResult gains a trailing batch axis, and batch[b] is a result of views onto slice b.\n\nOne allocation per output for the whole batch rather than one per field.\n\n\n\n\n\n","title":"HelmholtzDecomposition.HelmholtzBatch"},{"category":"function","location":"api/#HelmholtzDecomposition.allocate_batch","page":"API","text":"allocate_batch(plan, nfields) -> HelmholtzBatch\n\n\n\n\n\n","title":"HelmholtzDecomposition.allocate_batch"},{"category":"function","location":"api/#HelmholtzDecomposition.streamfunction","page":"API","text":"streamfunction(result) -> Array\n\nThe 2-D streamfunction ψ, on the corner (dual) grid. Only defined in 2D.\n\n\n\n\n\n","title":"HelmholtzDecomposition.streamfunction"},{"category":"function","location":"api/#HelmholtzDecomposition.velocity_potential","page":"API","text":"velocity_potential(result) -> Array\n\nThe scalar velocity potential χ, at cell centres, in any dimension.\n\n\n\n\n\n","title":"HelmholtzDecomposition.velocity_potential"},{"category":"function","location":"api/#HelmholtzDecomposition.vector_potential","page":"API","text":"vector_potential(result) -> (A1, A2, A3)\n\nThe 3-D vector potential, the Hodge dual of the rotation potential: A1 = R_23, A2 = −R_13, A3 = R_12. Only defined in 3D.\n\n\n\n\n\n","title":"HelmholtzDecomposition.vector_potential"},{"category":"function","location":"api/#HelmholtzDecomposition.count_holes","page":"API","text":"count_holes(grid) -> Int\n\nNumber of inactive regions fully enclosed by active cells — an estimate of the first Betti number b₁ of the active region, i.e. the dimension of the harmonic subspace.\n\nThis is what makes harmonic_fraction readable: a large harmonic part on a domain with count_holes == 0 is a boundary-circulation effect, while on one with holes it is the circulation around them, which no single-valued potential can represent.\n\nThe flood fill is FlowGeometries.Connectivity.connected_components, which walks the grid's own wrapping — so on a periodic direction a region running off one side and back on the other is one region, and \"touching the boundary\" is asked only of directions that actually have one.\n\n\n\n\n\n","title":"HelmholtzDecomposition.count_holes"},{"category":"function","location":"api/#HelmholtzDecomposition.face_velocity","page":"API","text":"face_velocity(ws), face_divergent(ws), face_rotational(ws) -> NTuple{N,Array}\n\nThe staggered fields the last decomposition through ws computed: one face-normal array per direction, nfaces(grid, d) long where the direction ends and n where it wraps.\n\nThese are where the projection is exact. face_divergent reproduces the input's divergence to round-off and face_rotational is divergence-free to round-off, both discretely; the collocated u_div and u_rot on the result are these interpolated back to cell centres, and that round trip is the entire harmonic residue a smooth field shows — 9.6e-3 at n = 32, falling at second order. A caller who works on the C-grid takes these and has no interpolation error at all.\n\nValid until the next decomposition through the same workspace, which overwrites them.\n\n\n\n\n\n","title":"HelmholtzDecomposition.face_velocity"},{"category":"function","location":"api/#HelmholtzDecomposition.face_divergent","page":"API","text":"face_divergent(ws) -> NTuple{N,Array}\n\nGχ on faces — see face_velocity.\n\n\n\n\n\n","title":"HelmholtzDecomposition.face_divergent"},{"category":"function","location":"api/#HelmholtzDecomposition.face_rotational","page":"API","text":"face_rotational(ws) -> NTuple{N,Array}\n\n−δR on faces, divergence-free to round-off — see face_velocity.\n\n\n\n\n\n","title":"HelmholtzDecomposition.face_rotational"},{"category":"function","location":"api/#HelmholtzDecomposition.corner_rotation_potential","page":"API","text":"corner_rotation_potential(ws) -> NTuple{P,Array}\n\nR on the corner (dual) grid, one array per rotation pair — see face_velocity.\n\n\n\n\n\n","title":"HelmholtzDecomposition.corner_rotation_potential"},{"category":"type","location":"api/#HelmholtzDecomposition.AbstractBoundaryCondition","page":"API","text":"AbstractBoundaryCondition\n\nSupertype for conditions on a bounded direction. Concrete: Dirichlet, Neumann.\n\n\n\n\n\n","title":"HelmholtzDecomposition.AbstractBoundaryCondition"},{"category":"type","location":"api/#HelmholtzDecomposition.Dirichlet","page":"API","text":"Homogeneous Dirichlet condition (Φ = 0 on the boundary).\n\n\n\n\n\n","title":"HelmholtzDecomposition.Dirichlet"},{"category":"type","location":"api/#HelmholtzDecomposition.Neumann","page":"API","text":"Homogeneous Neumann condition (∂Φ/∂n = 0 on the boundary): no flux through the domain edge.\n\n\n\n\n\n","title":"HelmholtzDecomposition.Neumann"},{"category":"type","location":"api/#HelmholtzDecomposition.AbstractPoissonSolver","page":"API","text":"AbstractPoissonSolver\n\nSupertype for Poisson solvers. Concrete subtypes implement\n\nsolve_poisson!(Φ, RHS, grid, solver; boundary, kwargs...) -> SolverResult\n\n\n\n\n\n","title":"HelmholtzDecomposition.AbstractPoissonSolver"},{"category":"type","location":"api/#HelmholtzDecomposition.AutoSolver","page":"API","text":"AutoSolver()\n\nSentinel for automatic selection. The only thing permitted to choose a solver on the caller's behalf, and it chooses on real capability — geometry, node layout, mask, axis uniformity, the requested boundary condition, and which extensions are loaded.\n\n\n\n\n\n","title":"HelmholtzDecomposition.AutoSolver"},{"category":"type","location":"api/#HelmholtzDecomposition.CGSolver","page":"API","text":"CGSolver(; max_iter = 1000, rtol = 1e-10)\n\nConjugate gradients on −L, which is symmetric positive (semi)definite by construction — see Operators.jl. Jacobi-preconditioned, and on a closed problem the iterate and residual are kept orthogonal to the constants.\n\nWorks on any grid, any mask, any boundary condition. Stage 6 adds a multigrid preconditioner in place of Jacobi; the outer iteration is unchanged by that.\n\n\n\n\n\n","title":"HelmholtzDecomposition.CGSolver"},{"category":"function","location":"api/#HelmholtzDecomposition.solve_poisson!","page":"API","text":"solve_poisson!(Φ, RHS, grid, solver; boundary, coefficients, state, backend) -> SolverResult\n\nSolve L Φ = RHS in place, with L the operator Operators.jl builds.\n\ncoefficients and state are the reusable parts — the Hodge factors of every face, and whatever the solver prepared for this (grid, boundary) pair. A decomposition solves P + 1 right-hand sides and a batch multiplies that by the field count, so both are built once and passed in rather than derived here.\n\nExtensions add methods for their own solver types; the iterative CGSolver is the one that works on any grid, mask and boundary condition.\n\n\n\n\n\n","title":"HelmholtzDecomposition.solve_poisson!"},{"category":"function","location":"api/#HelmholtzDecomposition.prepare_solver","page":"API","text":"prepare_solver(solver, grid, boundary) -> state\n\nWhatever solver can compute once for a (grid, boundary) pair and reuse on every solve — returned opaquely and handed back to solve_poisson! as its state keyword.\n\nThis exists because the transform plans are the dominant per-call cost and depend on nothing that changes between calls. A decomposition solves P + 1 right-hand sides on one grid and a batch multiplies that by the number of fields, so a solver planning inside solve_poisson! rebuilds the same plan (P + 1) · B times: in 3-D that is 17 FFTW plans per field, and for the non-uniform transforms a plan and a node upload per conjugate-gradient iteration.\n\nnothing is the default and means \"nothing to reuse\", which is correct for the iterative solvers — their reusable part is the LaplacianCoefficients, which the plan already holds.\n\n\n\n\n\n","title":"HelmholtzDecomposition.prepare_solver"},{"category":"function","location":"api/#HelmholtzDecomposition.select_solver","page":"API","text":"select_solver(solver, grid, boundary) -> AbstractPoissonSolver\n\nThe concrete solver for this problem, resolved and validated once. Resolution reads the mask, so a caller with several right-hand sides on one grid — which a decomposition always has, P + 1 of them — calls this once and hands the result to each solve.\n\n\n\n\n\n","title":"HelmholtzDecomposition.select_solver"},{"category":"type","location":"api/#HelmholtzDecomposition.SolverResult","page":"API","text":"SolverResult{T}\n\nConvergence diagnostics from a solve: whether it met its tolerance, how many iterations it took (1 for a direct or spectral solve), and the final residual.\n\n\n\n\n\n","title":"HelmholtzDecomposition.SolverResult"},{"category":"function","location":"api/#HelmholtzDecomposition.supports_boundary","page":"API","text":"supports_boundary(solver, boundary) -> Bool\n\nWhether solver solves the problem boundary describes. A solver that does not is never selected by AutoSolver and errors when named directly — solving a different problem from the one asked for is indistinguishable from a correct answer at the call site.\n\n\n\n\n\n","title":"HelmholtzDecomposition.supports_boundary"},{"category":"function","location":"api/#HelmholtzDecomposition.requires_full_domain","page":"API","text":"requires_full_domain(solver) -> Bool\n\nWhether solver needs every cell active. The spectral solvers transform the whole array, so a masked cell would be transformed as though it held data; they set this and are refused a masked grid rather than returning a field that is wrong wherever the mask bites.\n\n\n\n\n\n","title":"HelmholtzDecomposition.requires_full_domain"},{"category":"function","location":"api/#HelmholtzDecomposition.requires_uniform_axes","page":"API","text":"requires_uniform_axes(solver) -> Bool\n\nWhether solver needs constant spacing in every direction. An FFT-based solver does; the question is answered from the axis TYPE by FlowGeometries, at no runtime cost.\n\n\n\n\n\n","title":"HelmholtzDecomposition.requires_uniform_axes"},{"category":"function","location":"api/#HelmholtzDecomposition.requires_periodic_domain","page":"API","text":"requires_periodic_domain(solver) -> Bool\n\nWhether solver needs every direction to wrap. An FFT expands in a periodic basis, so it solves the whole-torus problem and nothing else.\n\nThis is a question about the grid's topology, not about a boundary condition — which is why there is no Periodic boundary condition to ask instead. A direction that wraps has no boundary to impose a condition on, so on a grid this solver accepts, boundary is vacuous rather than honoured or refused.\n\n\n\n\n\n","title":"HelmholtzDecomposition.requires_periodic_domain"},{"category":"function","location":"api/#HelmholtzDecomposition.register_spectral_solver!","page":"API","text":"register_spectral_solver!(algorithm, solver_type; priority)\n\nDeclare that solver_type implements algorithm. Lower priority is tried first: a native implementation takes a lower number than a generic one that would also work.\n\n\n\n\n\n","title":"HelmholtzDecomposition.register_spectral_solver!"},{"category":"function","location":"api/#HelmholtzDecomposition.gradient!","page":"API","text":"gradient!(g, χ, grid, bc) -> g\n\ng[d][F] = (χ_above − χ_below) / gap on every face. A face with area but only one cell is a Dirichlet edge, whose ghost value is zero — which is exactly what the condition says.\n\n\n\n\n\n","title":"HelmholtzDecomposition.gradient!"},{"category":"function","location":"api/#HelmholtzDecomposition.divergence!","page":"API","text":"divergence!(δ, v, grid, bc) -> δ\n\nδ[I] = (1/V_I) Σ_d (A·v)[face above] − (A·v)[face below] — the negative adjoint of gradient! under the cell-measure inner product, which is what makes L = D G symmetric.\n\n\n\n\n\n","title":"HelmholtzDecomposition.divergence!"},{"category":"function","location":"api/#HelmholtzDecomposition.laplacian!","page":"API","text":"laplacian!(out, χ, grid, bc, scratch) -> out\n\nL χ = D G χ, from exactly the two operators above, so a solver inverts the same L the decomposition differentiates with.\n\n\n\n\n\n","title":"HelmholtzDecomposition.laplacian!"},{"category":"function","location":"api/#HelmholtzDecomposition.apply_laplacian!","page":"API","text":"apply_laplacian!(out, Φ, grid, c) -> out\n\nout = L Φ, from the prebuilt coefficients. A face at the outer edge of a bounded direction has no cell beyond it and contributes c·(0 − Φ_I) — the zero ghost a Dirichlet condition places there. Under Neumann that face's coefficient is zero and the term vanishes, so one expression serves both.\n\n\n\n\n\n","title":"HelmholtzDecomposition.apply_laplacian!"},{"category":"function","location":"api/#HelmholtzDecomposition.curl!","page":"API","text":"curl!(W, v, grid, bc) -> W\n\nW_ab = ∂_a v_b − ∂_b v_a on the (a,b) corner, from face-normal velocity.\n\nThe circulation v·g is differenced rather than v itself, so what is differenced is the metric-free d; the metric re-enters once, in the division at the end. A corner is included only when all four faces bounding it are open — a loop running half through a mask edge has no zero circulation, and requiring only two faces is what previously broke curl(grad χ) = 0 on a masked grid.\n\n\n\n\n\n","title":"HelmholtzDecomposition.curl!"},{"category":"function","location":"api/#HelmholtzDecomposition.rotational_velocity!","page":"API","text":"rotational_velocity!(u_rot, R, grid, bc) -> u_rot\n\nu_rot_a = −Σ_b ∂_b R_ab, moving each component of R from its corner onto the a-faces. The adjoint of curl!, so div(u_rot) = 0 holds discretely.\n\n\n\n\n\n","title":"HelmholtzDecomposition.rotational_velocity!"},{"category":"function","location":"api/#HelmholtzDecomposition.to_faces!","page":"API","text":"to_faces!(vf, uc, grid, bc) -> vf\n\nCell-centred velocity (dims..., N) to face-normal velocity, averaging the two cells a face separates. A face of zero area takes nothing; an open boundary face has only one cell to read.\n\n\n\n\n\n","title":"HelmholtzDecomposition.to_faces!"},{"category":"function","location":"api/#HelmholtzDecomposition.to_centres!","page":"API","text":"to_centres!(uc, vf, grid, bc) -> uc\n\nFace-normal velocity back to cell centres, averaging a cell's two faces — the adjoint of to_faces!. A cell against a closed face averages only the faces that carry flux.\n\n\n\n\n\n","title":"HelmholtzDecomposition.to_centres!"},{"category":"type","location":"api/#HelmholtzDecomposition.LaplacianCoefficients","page":"API","text":"LaplacianCoefficients{N,T,A,B}\n\nThe per-face coefficients c_f = A_f / g_f of L, and the cell measures, evaluated once for a (grid, boundary) pair.\n\nEvery iterative solve applies L repeatedly, and each application would otherwise re-evaluate the geometry: face areas involve the scale factors at the face, which on a curved geometry means trigonometry per cell per direction. That is invariant across iterations — and across the P + 1 potentials and the whole batch — so it is reduced once here.\n\ncoef[I, d] is the coefficient of the face below cell I along d; diag[I] is the negative sum of a cell's own face coefficients, divided by its measure.\n\n\n\n\n\n","title":"HelmholtzDecomposition.LaplacianCoefficients"},{"category":"function","location":"api/#HelmholtzDecomposition.laplacian_coefficients","page":"API","text":"laplacian_coefficients(grid, bc) -> LaplacianCoefficients\n\nThe Hodge factor c_F = A_F / g_F of every face, and the cell measures, reduced once for a (grid, boundary) pair.\n\nEvery iterative solve applies L repeatedly, and each application would otherwise re-evaluate the geometry — face areas involve the scale factors at the face, i.e. trigonometry per cell per direction on a curved grid. That is invariant across iterations, across the P + 1 potentials, and across a whole batch, so it is computed once here and only read afterwards.\n\n\n\n\n\n","title":"HelmholtzDecomposition.laplacian_coefficients"},{"category":"type","location":"api/#HelmholtzDecomposition.FaceMetrics","page":"API","text":"FaceMetrics{N,T,A}\n\nEvery face's flux-carrying area and centre-to-centre gap, evaluated once for a (grid, boundary) pair.\n\nface_area and face_gap each read the grid's coordinates and evaluate the geometry's scale factors at the face — on a curved grid, trigonometry per face per direction. None of it depends on the field, yet the operators call them inside their loops, so a decomposition recomputed the same metric 2N + 4P times per field and again for every field of a batch. Reduced here to two array reads.\n\n\n\n\n\n","title":"HelmholtzDecomposition.FaceMetrics"},{"category":"function","location":"api/#HelmholtzDecomposition.face_area","page":"API","text":"face_area(grid, F, d, bc, T) -> T\n\nThe area that carries flux across face F — the coefficient the operators use, and where the boundary condition lives, because on a flux-form operator that is what a boundary condition is:\n\nbetween two active cells → the geometric area;\nagainst a masked-out cell → 0. No flux crosses, which is at once the no-flux condition, the mask treatment, and the reason D = −G* survives both;\nat the outer edge of a bounded direction → 0 under Neumann, the geometric area under Dirichlet, whose ghost value beyond the edge is zero.\n\nOn a covering spherical grid the poles need no special case: the φ-face area carries the cos φ that vanishes there, so the metric closes the surface itself.\n\n\n\n\n\n","title":"HelmholtzDecomposition.face_area"},{"category":"function","location":"api/#HelmholtzDecomposition.face_gap","page":"API","text":"face_gap(grid, F, d, T) -> T\n\nPhysical centre-to-centre distance across face F: the mean of the two cells' physical widths, or half a cell where the face has only one. Consistent with face_area — a great-circle distance would not be, being shorter than the coordinate line the area is built on.\n\n\n\n\n\n","title":"HelmholtzDecomposition.face_gap"},{"category":"function","location":"api/#HelmholtzDecomposition.nfaces","page":"API","text":"nfaces(grid, d) -> Int\n\nNumber of faces normal to direction d: n if the direction wraps, n + 1 if it ends.\n\n\n\n\n\n","title":"HelmholtzDecomposition.nfaces"},{"category":"function","location":"api/#HelmholtzDecomposition.ncorners","page":"API","text":"ncorners(grid, d) -> Int\n\nCorners along a staggered direction: n where it wraps, n - 1 where it ends.\n\nA bounded direction has n + 1 faces but only n - 1 corners that carry an unknown. The two outermost have no closed loop of cells around them, so the circulation there is not defined and R is not solved for — it is zero. Leaving them in the array and masking them off says the same thing, but says it as data, and a masked grid is refused by every direct transform. Dropping them says it as a Dirichlet condition on a smaller domain, which a sine transform inverts exactly. Measured at 512²: 98 iterations and 4.6 s became one transform and 19 ms.\n\n\n\n\n\n","title":"HelmholtzDecomposition.ncorners"},{"category":"function","location":"api/#HelmholtzDecomposition.corner_offset","page":"API","text":"corner_offset(grid, d) -> Int\n\nWhat to add to a corner index in direction d to get the face index it sits on: 1 where the outer pair was dropped, 0 where the direction wraps and nothing was.\n\n\n\n\n\n","title":"HelmholtzDecomposition.corner_offset"},{"category":"function","location":"api/#HelmholtzDecomposition.face_dims","page":"API","text":"face_dims(grid, d) -> NTuple{N,Int}\n\nShape of the face array normal to direction d.\n\n\n\n\n\n","title":"HelmholtzDecomposition.face_dims"},{"category":"function","location":"api/#HelmholtzDecomposition.corner_dims","page":"API","text":"corner_dims(grid, a, b) -> NTuple{N,Int}\n\nShape of the array holding a rotation-potential component staggered in both a and b.\n\n\n\n\n\n","title":"HelmholtzDecomposition.corner_dims"},{"category":"function","location":"api/#HelmholtzDecomposition.allocate_faces","page":"API","text":"allocate_faces(T, grid) -> NTuple{N,Array{T,N}}\n\nOne zeroed face array per direction. This is the internal staggered layout; the public API stays collocated, so nothing outside this package sees it.\n\n\n\n\n\n","title":"HelmholtzDecomposition.allocate_faces"},{"category":"function","location":"api/#HelmholtzDecomposition.allocate_corners","page":"API","text":"allocate_corners(T, grid; backend) -> NTuple{P,AbstractArray{T,N}}\n\nOne zeroed corner array per rotation pair, shaped by corner_dims.\n\n\n\n\n\n","title":"HelmholtzDecomposition.allocate_corners"},{"category":"function","location":"api/#HelmholtzDecomposition.rotation_pairs","page":"API","text":"rotation_pairs(Val(N)) -> NTuple{P,Tuple{Int,Int}}\n\nThe pairs (a, b), a < b, lexicographically — the independent components of an antisymmetric 2-tensor, and equally the directions each component is staggered in.\n\n\n\n\n\n","title":"HelmholtzDecomposition.rotation_pairs"},{"category":"function","location":"api/#HelmholtzDecomposition.rotation_terms","page":"API","text":"rotation_terms(Val(N)) -> NTuple{N,NTuple{N-1,Tuple{Int,Int,Int}}}\n\nFor each direction c, the (e, p, s) triples saying how u_rot_c is assembled: difference component p of R along direction e with sign s. Inverting rotation_pairs this way turns the accumulation u_rot_a −= ∂_b R_ab, u_rot_b += ∂_a R_ab — a scatter, where several pairs write the same face — into a gather, so each face is written exactly once and the loop can be handed to Execution unchanged. Homogeneous, so indexing it with a runtime direction is stable.\n\n\n\n\n\n","title":"HelmholtzDecomposition.rotation_terms"},{"category":"function","location":"api/#HelmholtzDecomposition.n_rotation_components","page":"API","text":"n_rotation_components(N) -> Int\n\nN(N-1)/2: 0 in 1D, 1 in 2D, 3 in 3D.\n\n\n\n\n\n","title":"HelmholtzDecomposition.n_rotation_components"},{"category":"function","location":"api/#HelmholtzDecomposition.face_coordinates","page":"API","text":"face_coordinates(grid, d) -> axis\n\nCoordinates of the faces normal to direction d — the midpoints between consecutive cell centres, plus the two outer edges where the direction is bounded.\n\nA periodic direction gets n of them and a bounded one n + 1, matching nfaces.\n\nA uniform axis returns a range. Uniformly spaced cells have uniformly spaced faces, but FlowGeometries proves uniformity from the axis TYPE, so returning a Vector here would make every dual grid report itself as stretched — and requires_uniform_axes would then refuse the direct transform on a grid that qualifies for it. That is not a small loss: it left the rotation-potential solve iterative at every size, and it was the whole cost of a 512² decomposition.\n\n\n\n\n\n","title":"HelmholtzDecomposition.face_coordinates"},{"category":"function","location":"api/#HelmholtzDecomposition.corner_mask","page":"API","text":"corner_mask(grid, a, b) -> Array{Bool}\n\nWhich (a,b) corners have a closed loop of active cells around them — the same four-face test curl! applies, hoisted so the dual grid carries it.\n\nA corner without a closed loop is not in the complex: the circulation around it is not defined, so R there is not solved for and contributes nothing back.\n\n\n\n\n\n","title":"HelmholtzDecomposition.corner_mask"},{"category":"function","location":"api/#HelmholtzDecomposition.dual_grid","page":"API","text":"dual_grid(grid, a, b, bc) -> StructuredGrid\n\nThe grid the (a,b) rotation-potential component lives on: corner coordinates in directions a and b, cell coordinates elsewhere, the primal's geometry and per-direction topology, and corner_mask as its mask.\n\nA bounded direction contributes its interior corners only — see ncorners. The pair it drops is where R is pinned to zero, which the dual solve states as Dirichlet rather than as a mask, so an unmasked domain stays unmasked here and keeps its direct transform.\n\n\n\n\n\n","title":"HelmholtzDecomposition.dual_grid"},{"category":"function","location":"api/#HelmholtzDecomposition.execution_backend","page":"API","text":"execution_backend(backend)\n\nThe object FlowGeometries.Execution dispatches on, given a ComputationalBackends one.\n\nThe two libraries answer different questions: ComputationalBackends names what kind of execution is wanted, while Execution's device methods dispatch on the KernelAbstractions device object itself. A GPUBackend therefore hands over the device it names; serial and threaded backends pass through, FlowGeometries' own ComputationalBackends extension having methods for them. Anything else reaches run_indices unchanged and raises a MethodError there rather than running serially in silence.\n\n\n\n\n\n","title":"HelmholtzDecomposition.execution_backend"},{"category":"function","location":"api/#HelmholtzDecomposition.resolve_execution_backend","page":"API","text":"resolve_execution_backend(backend) -> concrete backend\n\nThe backend a single decomposition's own loops run on, resolved once so nothing below has to ask.\n\nAutoBackend reads the thread count — the defect that made every documented parallel path dead at defaults was resolving it to serial unconditionally. A GPUBackend is checked here, at plan time, against whether FlowGeometries' KernelAbstractions extension is actually loaded, so the error names the package to load rather than surfacing as a MethodError from inside a loop.\n\nDistributedBackend and MPIBackend are refused: they spread fields across processes, which is helmholtz_decompose_batch's axis, not the index space inside one field.\n\n\n\n\n\n","title":"HelmholtzDecomposition.resolve_execution_backend"},{"category":"function","location":"api/#HelmholtzDecomposition.allocate_zeros","page":"API","text":"allocate_zeros(backend, T, dims) -> zeroed array\n\nEvery buffer this package owns goes through here, so that a device backend gets device memory rather than a host array a kernel cannot reach. The default is a host Array; the KernelAbstractions extension adds the device method.\n\n\n\n\n\n","title":"HelmholtzDecomposition.allocate_zeros"},{"category":"function","location":"api/#HelmholtzDecomposition.to_backend","page":"API","text":"to_backend(backend, x) -> x on the backend's memory\n\nMove an already-built array — or a struct of them — to where backend executes.\n\nThe plan's coefficients and face metrics are built by walking the grid's geometry, which is host work done once; only their results are read in the inner loops. So they are assembled on the host and moved here, rather than every geometry accessor being made device-callable to build them in place. Identity by default; the KernelAbstractions extension routes it through Adapt.\n\n\n\n\n\n","title":"HelmholtzDecomposition.to_backend"},{"category":"function","location":"api/#HelmholtzDecomposition.multigrid","page":"API","text":"multigrid(grid, bc, T; ω = 0.8, ν = 2, maxlevels = 12) -> MultigridPreconditioner\n\nBuild the hierarchy. ω = 0.8 is the usual damped-Jacobi factor for a 2-D five-point Laplacian — undamped Jacobi does not reduce the highest-frequency error at all, which is the error a smoother exists to remove.\n\n\n\n\n\n","title":"HelmholtzDecomposition.multigrid"},{"category":"type","location":"api/#HelmholtzDecomposition.MultigridPreconditioner","page":"API","text":"MultigridPreconditioner\n\nA hierarchy built once for a (grid, boundary) pair, applied as z ← M⁻¹ r inside conjugate gradients.\n\n\n\n\n\n","title":"HelmholtzDecomposition.MultigridPreconditioner"},{"category":"type","location":"api/#HelmholtzDecomposition.MultigridLevel","page":"API","text":"MultigridLevel\n\nOne rung: the grid, its Laplacian coefficients, and the buffers a cycle needs there.\n\n\n\n\n\n","title":"HelmholtzDecomposition.MultigridLevel"},{"category":"function","location":"api/#HelmholtzDecomposition.coarsen","page":"API","text":"coarsen(grid, bc) -> grid or nothing\n\nThe next grid down: every other coordinate in each direction that still has enough cells to halve.\n\nA coarse cell is active when any of the fine cells it covers is. The alternative — requiring all of them — erodes the domain by a cell per level, so a narrow channel or a coastline would vanish partway down the hierarchy and the correction there would be identically zero.\n\n\n\n\n\n","title":"HelmholtzDecomposition.coarsen"},{"category":"function","location":"api/#HelmholtzDecomposition.galerkin_coefficients","page":"API","text":"galerkin_coefficients(fine, fgrid, cgrid) -> LaplacianCoefficients\n\nThe coarse operator as R A P rather than a fresh discretization of the coarse grid.\n\nRe-discretizing is what breaks a masked domain. The coarse mask cannot reproduce the fine one — mark a coarse cell active if any child is and the domain grows outward a cell per level, require all and it erodes — so the coarse operator ends up solving a differently-shaped problem and its correction is inconsistent with the fine one. That showed up as the rotation-potential solves running to their iteration cap while the primal solve converged in 13.\n\nFor this transfer pair — restriction that averages a cell's children, prolongation that adds a coarse value to each of them — the Galerkin product has a closed form and needs no matrix: the conductance of a coarse face is the sum of the fine conductances crossing it, and a coarse cell's measure is the sum of its children's. Nothing about the geometry is recomputed, so nothing can disagree with the fine level.\n\n\n\n\n\n","title":"HelmholtzDecomposition.galerkin_coefficients"},{"category":"function","location":"api/#HelmholtzDecomposition.restrict!","page":"API","text":"restrict!(coarse, fine, cgrid, fgrid, fc, cc)\n\nMeasure-weighted average of each coarse cell's fine children.\n\nThe weighting is not a refinement — it is what makes R the adjoint of prolongation, and a non-adjoint pair makes the preconditioner non-symmetric, which invalidates conjugate gradients. With prolongation by injection and the coarse measure defined as the sum of its children's,\n\n⟨P c, f⟩_fine = Σ_I Σ_{J∈children(I)} V_J c_I f_J = ⟨c, R f⟩_coarse\n⟹  (R f)_I = Σ_J V_J f_J / Σ_J V_J\n\nAn unweighted average satisfies that only when every V_J is equal, which is why it worked on a Cartesian grid and failed outright on a sphere, where the measure varies as cos φ: multigrid ran to the iteration cap at every resolution while plain Jacobi converged.\n\nNo mask term is needed — an inactive cell's measure is already zero, so it carries no weight.\n\n\n\n\n\n","title":"HelmholtzDecomposition.restrict!"},{"category":"function","location":"api/#HelmholtzDecomposition.prolong_add!","page":"API","text":"prolong_add!(fine, coarse, cgrid, fgrid, fc)\n\nAdd each coarse value to the fine cells it covers — injection, the adjoint of the measure-weighted restrict!, which is what keeps the cycle symmetric.\n\n\n\n\n\n","title":"HelmholtzDecomposition.prolong_add!"},{"category":"function","location":"api/#HelmholtzDecomposition.smooth!","page":"API","text":"smooth!(x, b, level, ω, n)\n\nn sweeps of damped Jacobi on −L, the positive-definite orientation.\n\nDamped Jacobi rather than Gauss–Seidel: it is a pure Ax plus an axpy, so it is the same expression serial, threaded and on a device, and it is symmetric — which Gauss–Seidel is not unless the sweep order is reversed between the pre- and post-smoothing, and a non-symmetric preconditioner breaks conjugate gradients.\n\n\n\n\n\n","title":"HelmholtzDecomposition.smooth!"},{"category":"function","location":"api/#HelmholtzDecomposition.vcycle!","page":"API","text":"vcycle!(levels, ω, ν, backend)\n\nOne V-cycle over levels, coarsest last: smooth, restrict the residual, recurse on the tail, prolong the correction, smooth again. Equal pre- and post-smoothing keeps the cycle symmetric.\n\nThe recursion is on the tuple's tail, not on an index. Recursing with Val(l + 1) gives the compiler no proof the depth terminates, so it abandons inference at the recursive call and boxes the arguments — 1 KiB per level per cycle, which at 81 conjugate-gradient iterations was most of a megabyte per solve. A shorter tuple type each step terminates structurally.\n\n\n\n\n\n","title":"HelmholtzDecomposition.vcycle!"},{"category":"function","location":"api/#HelmholtzDecomposition.helmholtz_project_spectral!","page":"API","text":"helmholtz_project_spectral!(û_rot, û_div, û_harm, velocity_hat, ks::NTuple{N})\n\nIn-place Leray projection. velocity_hat and the three outputs are component-last spectral arrays of size (kdims..., N); ks holds the per-axis wavenumber vectors. Writes the rotational (divergence-free) part into û_rot, the divergent (curl-free) part into û_div, and the k = 0 mode into û_harm, so that the three sum to velocity_hat.\n\nThe k = 0 mode is separated rather than left in û_rot because a constant field is both curl-free and divergence-free: it is the harmonic part, not the rotational one. GPU-compatible (pure broadcast).\n\n\n\n\n\n","title":"HelmholtzDecomposition.helmholtz_project_spectral!"},{"category":"function","location":"api/#HelmholtzDecomposition.helmholtz_project_spectral","page":"API","text":"helmholtz_project_spectral(velocity_hat, ks::NTuple) -> SpectralCartesianResult\n\nAllocating Leray projection from a component-last spectral array.\n\n\n\n\n\n","title":"HelmholtzDecomposition.helmholtz_project_spectral"},{"category":"function","location":"api/#HelmholtzDecomposition.helmholtz_potentials_spectral","page":"API","text":"helmholtz_potentials_spectral(velocity_hat, ks::NTuple{N}) -> (χ_hat, R_hat)\n\nCompute the spectral scalar velocity potential χ_hat (size (kdims...)) and the rotation-potential components R_hat (component-last, size (kdims..., P), P = N(N-1)/2) from a component-last spectral velocity array. Uses the spectral Poisson inverses χ̂ = −i (k·û)/k² and R̂_ab = −i (k_a û_b − k_b û_a)/k², with the k = 0 mode set to zero.\n\n\n\n\n\n","title":"HelmholtzDecomposition.helmholtz_potentials_spectral"},{"category":"function","location":"api/#HelmholtzDecomposition.helmholtz_decompose_spectral","page":"API","text":"helmholtz_decompose_spectral(u, grid; kwargs...)\nhelmholtz_decompose_spectral(u, v, grid; kwargs...)        # 2D convenience\nhelmholtz_decompose_spectral(u, v, w, grid; kwargs...)     # 3D convenience\n\nDecompose a physical velocity field on grid using a spectral transform, returning a physical HelmholtzResult (CPU) — or, on the GPU path, a (; u_rot, u_div, u_harm) NamedTuple of CuArrays. Requires the appropriate extension (using FFTW, using FastSphericalHarmonics, …). Pass solver= to select among loaded spectral backends.\n\nFor raw spectral coefficients, use the lower-level helmholtz_project_spectral.\n\n\n\n\n\n","title":"HelmholtzDecomposition.helmholtz_decompose_spectral"},{"category":"function","location":"api/#HelmholtzDecomposition.velocity_norm","page":"API","text":"velocity_norm(U, grid, scratch) -> T\n\nMeasure-weighted ‖U‖ = sqrt(∫|U|² dV) over the active cells.\n\n\n\n\n\n","title":"HelmholtzDecomposition.velocity_norm"},{"category":"function","location":"api/#HelmholtzDecomposition.project_out_constant!","page":"API","text":"project_out_constant!(Φ, grid, c)\n\nRemove the measure-weighted mean of Φ over the active cells.\n\nA closed problem — every direction periodic, or a Neumann boundary — leaves the constants in L's null space. Krylov iterations must stay orthogonal to that null space or they drift along it, so this is applied to the right-hand side once and to the iterate as it goes.\n\n\n\n\n\n","title":"HelmholtzDecomposition.project_out_constant!"},{"category":"section","location":"theory/#Theory","page":"Theory","text":"","title":"Theory"},{"category":"section","location":"theory/#The-Commutativity-Problem-on-the-Sphere","page":"Theory","text":"Aluie (2019, doi:10.1007/s13137-019-0123-9) proves that on the sphere S², filtering vector fields by converting to Cartesian components and filtering each as a scalar does NOT commute with differential operators:\n\nG * (∇·u) ≠ ∇·(G * u)    on S²\nG * (∇×u) ≠ ∇×(G * u)    on S²\n\nThis means that filtering a divergence-free velocity field via Cartesian components does NOT produce a divergence-free filtered field.","title":"The Commutativity Problem on the Sphere"},{"category":"section","location":"theory/#The-Helmholtz-Solution","page":"Theory","text":"Aluie (2019, Section 7, Proposition 2) proves that the generalized convolution that DOES commute with derivatives is mathematically equivalent to:\n\nDecomposing u into scalar potentials: u → (ψ, χ, u_r)\nFiltering each scalar separately: ψ̄, χ̄, ū_r\nReconstructing velocity from filtered potentials\n\nThis is the Edmonds transformation (Edmonds 1960).","title":"The Helmholtz Solution"},{"category":"section","location":"theory/#The-full-Helmholtz–Hodge-decomposition","page":"Theory","text":"On a domain Ω ⊂ ℝⁿ, a vector field admits the L²-orthogonal decomposition\n\nu  =  ∇χ  ⊕  rot R  ⊕  h\n\ninto a curl-free part ∇χ (divergent), a divergence-free part rot R (rotational), and a harmonic part h that is both divergence- and curl-free. On a periodic torus or an unbounded domain with decay, dim of the harmonic space is 0 (only the mean, which the solvers remove). On a bounded or multiply-connected domain the harmonic space has dimension equal to the first Betti number b₁ of the domain (the number of holes/islands): h then carries the net circulation around each hole and the net flux through it, which no single-valued ψ/χ can represent.\n\nThe decomposition is not unique on a bounded domain without boundary conditions. The \"natural HHD\" (Bhatia et al. 2013) fixes uniqueness and orthogonality by assigning the normal trace to ∇χ (Neumann ∂χ/∂n = u·n̂) and the tangential trace to rot R (Dirichlet ψ = const per boundary component). This package computes h as the residual h = u − ∇χ − rot R after the potential solves (harmonic_fraction = ‖h‖/‖u‖ is reported), and detects topology with count_holes / betti1_estimate.","title":"The full Helmholtz–Hodge decomposition"},{"category":"section","location":"theory/#N-dimensional-potentials","page":"Theory","text":"The 3-D vector potential generalizes to an antisymmetric rotation-potential matrix R with N(N−1)/2 independent components (Glötzl & Richters 2023). The governing equations are component-wise Poisson problems,\n\nΔχ      = ∇·u                              (gradient/scalar potential)\nΔR_ab   = ∂_a u_b − ∂_b u_a   (a < b)      (rotation potential)\n\nwith the identity grad div u + ROT ROT̄ u = Δu generalizing grad div − curl curl = Δ. Per dimension: 1-D → pure gradient (no rotation); 2-D → one component R₁₂ = ψ (streamfunction); 3-D → three components, the Hodge dual of the vector potential A (u_rot = ∇×A); N ≥ 4 → only χ and the matrix R are well-defined (no ψ, no A). In Fourier space the velocity split is dimension-trivial — the Leray projection û_div = (k̂⊗k̂)û, û_rot = (I − k̂⊗k̂)û — which is the fast path used by the spectral solvers.","title":"N-dimensional potentials"},{"category":"section","location":"theory/#The-2D-special-case","page":"Theory","text":"Any 2D vector field can be written u = u_rot + u_div (+ harmonic), with u_rot = ∇×(ψ ẑ) non-divergent and u_div = ∇χ irrotational.","title":"The 2D special case"},{"category":"section","location":"theory/#Cartesian-Geometry","page":"Theory","text":"Given vorticity ζ = ∂v/∂x − ∂u/∂y and divergence δ = ∂u/∂x + ∂v/∂y:\n\nSolve ∇²ψ = ζ\nSolve ∇²χ = δ\nurot = −∂ψ/∂y, vrot = ∂ψ/∂x\nudiv = ∂χ/∂x, vdiv = ∂χ/∂y","title":"Cartesian Geometry"},{"category":"section","location":"theory/#Spherical-Geometry","page":"Theory","text":"On a sphere of radius R with coordinates (λ, φ):\n\nSolve ∇²ψ = ζ where ∇² is the spherical Laplacian\nu_rot = −(1/R) ∂ψ/∂φ\nv_rot = 1/(R cos φ) ∂ψ/∂λ\n\nThe spherical Laplacian has eigenvalues −ℓ(ℓ+1)/R² for spherical harmonic degree ℓ.","title":"Spherical Geometry"},{"category":"section","location":"theory/#Spectral-Poisson-Solvers","page":"Theory","text":"The Poisson equation can be solved in O(N log N) time using spectral methods:\n\nTransform RHS to spectral space (FFT or SHT)\nDivide each mode by its eigenvalue (−k² for FFT, −ℓ(ℓ+1)/R² for SHT)\nTransform back to physical space\n\nThe k=0 (or ℓ=0) mode is set to zero (the mean of the solution is arbitrary).","title":"Spectral Poisson Solvers"},{"category":"section","location":"theory/#References","page":"Theory","text":"Aluie, H. (2019). Convolutions on the sphere: commutation with differential operators. GEM - International Journal on Geomathematics, 10(1), 9. doi:10.1007/s13137-019-0123-9\nGlötzl, E., & Richters, O. (2023). Helmholtz decomposition and potential functions for n-dimensional analytic vector fields. Journal of Mathematical Analysis and Applications, 525(2), 127138. doi:10.1016/j.jmaa.2023.127138\nBhatia, H., Norgard, G., Pascucci, V., & Bremer, P.-T. (2013). The Helmholtz-Hodge Decomposition — A Survey. IEEE TVCG, 19(8), 1386–1404. doi:10.1109/TVCG.2012.316\nBuzzicotti, M., Storer, B. A., Khatri, H., Griffies, S. M., & Aluie, H. (2023). Spatio-temporal coarse-graining decomposition of the global ocean geostrophic kinetic energy. Science Advances, 9(45). doi:10.1126/sciadv.adi7420\nStorer, B. A., Buzzicotti, M., Khatri, H., Griffies, S. M., & Aluie, H. (2022). Global energy spectrum of the general oceanic circulation. Nature Communications, 13, 5314. doi:10.1038/s41467-022-33031-3","title":"References"},{"category":"section","location":"coarse_graining_workflow/#Coarse-Graining-Workflow","page":"Coarse-Graining Workflow","text":"","title":"Coarse-Graining Workflow"},{"category":"section","location":"coarse_graining_workflow/#The-Problem","page":"Coarse-Graining Workflow","text":"When coarse-graining (filtering) velocity fields on the sphere, the naive approach of:\n\nConvert (ueast, unorth) → (uX, uY, u_Z) planetary Cartesian\nFilter each Cartesian component as a scalar\nConvert back to spherical\n\ndoes NOT commute with differential operators (Aluie 2019). This means:\n\nFiltering a divergence-free field does NOT produce a divergence-free filtered field\nPhysical properties (incompressibility, etc.) are violated","title":"The Problem"},{"category":"section","location":"coarse_graining_workflow/#The-Correct-Approach","page":"Coarse-Graining Workflow","text":"The theoretically correct workflow (equivalent to the Edmonds transformation):\n\nOriginal velocity (u, v)\n    │\n    ▼\n┌─────────────────────────────┐\n│  Helmholtz Decomposition    │  ← This package\n│  Solve ∇²ψ = ζ, ∇²χ = δ    │\n└─────────────────────────────┘\n    │\n    ▼\nScalar potentials (ψ, χ)\n    │\n    ▼\n┌─────────────────────────────┐\n│  Filter ψ and χ separately  │  ← CoarseGrainingEnergyFluxes.jl\n│  as scalars on the sphere   │     (or any filtering package)\n└─────────────────────────────┘\n    │\n    ▼\nFiltered potentials (ψ̄, χ̄)\n    │\n    ▼\n┌─────────────────────────────┐\n│  Reconstruct velocity from  │\n│  filtered potentials        │\n└─────────────────────────────┘\n    │\n    ▼\nCorrectly filtered velocity (ū, v̄)\n    ✓ Commutes with ∇\n    ✓ Preserves incompressibility\n    ✓ Enables Π_tor / Π_pot decomposition","title":"The Correct Approach"},{"category":"section","location":"coarse_graining_workflow/#Integration-with-CoarseGrainingEnergyFluxes.jl","page":"Coarse-Graining Workflow","text":"using HelmholtzDecomposition: HelmholtzDecomposition\nusing CoarseGrainingEnergyFluxes: CoarseGrainingEnergyFluxes\nusing FFTW: FFTW  # or using NUFSHT: NUFSHT for spherical\n\n# Step 1: Helmholtz decomposition\nresult = HelmholtzDecomposition.helmholtz_decompose(cat(u, v; dims = 3), grid)\n\n# Step 2: Filter the scalar potentials\n# (CoarseGrainingEnergyFluxes provides the filtering machinery)\nψ_bar = filter_scalar(HelmholtzDecomposition.streamfunction(result), grid, ℓ)\nχ_bar = filter_scalar(result.χ, grid, ℓ)\n\n# Step 3: Compute energy flux from filtered Helmholtz scalars\n# This gives Π_tor, Π_pot and cross-terms (Buzzicotti 2023)","title":"Integration with CoarseGrainingEnergyFluxes.jl"},{"category":"section","location":"coarse_graining_workflow/#When-to-Skip-This","page":"Coarse-Graining Workflow","text":"Per Storer et al. (2022): if your velocity is already non-divergent (e.g., geostrophic velocity derived from SSH), the commutativity issue does not apply. The planetary Cartesian approach is correct in that case.\n\nOnly use the full Helmholtz workflow when:\n\nVelocity has both rotational AND divergent components\nYou need to decompose Π into toroidal/potential contributions\nYou need guaranteed commutativity for general velocity fields","title":"When to Skip This"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"","title":"Examples"},{"category":"section","location":"examples/#Visual-Results","page":"Examples","text":"","title":"Visual Results"},{"category":"section","location":"examples/#Taylor-Green-Vortex-(purely-rotational)","page":"Examples","text":"(Image: Taylor-Green Decomposition)","title":"Taylor-Green Vortex (purely rotational)"},{"category":"section","location":"examples/#Vortex-Source-(mixed-rotational-and-divergent)","page":"Examples","text":"(Image: Mixed Field Decomposition)","title":"Vortex + Source (mixed rotational and divergent)"},{"category":"section","location":"examples/#Point-Source-(purely-divergent)","page":"Examples","text":"(Image: Point Source Decomposition)","title":"Point Source (purely divergent)"},{"category":"section","location":"examples/#Harmonic-circulation-on-an-annulus-(multiply-connected)","page":"Examples","text":"(Image: Harmonic Annulus Decomposition)","title":"Harmonic circulation on an annulus (multiply-connected)"},{"category":"section","location":"examples/#3-D-ABC-(Beltrami)-flow-—-z-mid-slice","page":"Examples","text":"(Image: 3-D Decomposition)","title":"3-D ABC (Beltrami) flow — z mid-slice"},{"category":"section","location":"examples/#3-D-mixed-field-—-z-mid-slice","page":"Examples","text":"(Image: 3-D Mixed Decomposition)","title":"3-D mixed field — z mid-slice"},{"category":"section","location":"examples/#Spherical-mixed-flow-(Kelvin–Ekman,-NUFSHT)","page":"Examples","text":"(Image: Spherical Decomposition)\n\nVelocity-like result fields use a component-last layout (dims..., N); index the last axis (or use cat(u, v; dims=3) to build inputs). Runnable scripts live in examples/.","title":"Spherical mixed flow (Kelvin–Ekman, NUFSHT)"},{"category":"section","location":"examples/#Cartesian:-pure-rotational-field-(Taylor–Green-vortex)","page":"Examples","text":"using HelmholtzDecomposition: HelmholtzDecomposition as HD\nusing FlowGeometries: FlowGeometries as FG\nusing FFTW: FFTW\n\nN, L = 64, 1.0\naxis = FG.Axes.UniformAxis(0.0, L / N, N)\ngrid = FG.Grids.StructuredGrid(FG.Geometry.CartesianGeometry{Float64}(), axis, axis;\n                               topology = (true, true), period = (L, L))\n\nU = cat(u, v; dims = 3)                 # component-last (dims..., N)\nresult = HD.helmholtz_decompose(U, grid)\n\n@assert maximum(abs, result.u_div) < 1e-14      # divergent component ≈ 0\n\nUniformity is read from the axis TYPE, never by scanning values, so a collected axis is not provably uniform and no direct transform applies to it. UniformAxis is also isbits, so a grid built from one moves to a device for free.\n\nThe residue in result.harmonic_fraction on a smooth field is the two collocated↔staggered averaging hops and falls at second order — 9.6e-3 at N = 32, 6.0e-4 at N = 128. It is discretisation, not topology.","title":"Cartesian: pure rotational field (Taylor–Green vortex)"},{"category":"section","location":"examples/#Reusing-a-plan","page":"Examples","text":"helmholtz_decompose(u, grid) builds a plan, a workspace and a result and throws them away. For more than one field on a grid, build them once — the steady-state call then allocates nothing.\n\nplan = HD.plan_helmholtz(grid; boundary = HD.Neumann())\nws   = HD.allocate_workspace(plan)\nres  = HD.allocate_result(plan)\n\nfor u in fields\n    HD.helmholtz_decompose!(res, u, plan, ws)    # 0 bytes\nend","title":"Reusing a plan"},{"category":"section","location":"examples/#Batches","page":"Examples","text":"A HelmholtzBatch stores every output contiguously with the batch axis last, and batch[b] is a result of views onto slice b.\n\nusing OhMyThreads: OhMyThreads            # enables ThreadedBackend\nusing ComputationalBackends: ComputationalBackends as CB\n\nbatch = HD.helmholtz_decompose_batch(plan, fields; backend = CB.ThreadedBackend())\nsize(batch.u_rot)        # (dims..., N, length(fields))\nbatch[3].harmonic_fraction\n\nDistributed and MPI spread the same batch across processes or ranks. A backend that is named but whose extension is not loaded throws; it is never quietly downgraded to serial.","title":"Batches"},{"category":"section","location":"examples/#3-D:-ABC-(Beltrami)-flow","page":"Examples","text":"# U3 is component-last (Nx, Ny, Nz, 3)\nres3 = HD.helmholtz_decompose(U3, grid3)\nA1, A2, A3 = HD.vector_potential(res3)   # the 3-D vector potential","title":"3-D: ABC (Beltrami) flow"},{"category":"section","location":"examples/#Spherical:-Rossby-wave-(non-divergent)","page":"Examples","text":"NLON, NLAT = 128, 64\nλ = FG.Axes.UniformAxis(0.0, 2π / NLON, NLON)\nφ = FG.Axes.UniformAxis(-π / 2 + π / (2NLAT), π / NLAT, NLAT)\ngrid = FG.Grids.StructuredGrid(FG.Geometry.SphericalGeometry(6.371e6), λ, φ;\n                               topology = (true, false), period = (2π, nothing))\n\nresult = HD.helmholtz_decompose(cat(u, v; dims = 3), grid)\n\nA lat–lon grid is not a transform's node set, so it takes the multigrid-preconditioned iterative path. Load FastSphericalHarmonics for a Clenshaw–Curtis grid, or NUFSHT for any node set covering the sphere. A regional patch stays on the iterative solver: spherical-harmonic analysis integrates over all of S², so a patch does not determine the solution even on the patch.","title":"Spherical: Rossby wave (non-divergent)"},{"category":"section","location":"examples/#Coarse-Graining-Workflow","page":"Examples","text":"result = HD.helmholtz_decompose(cat(u, v; dims = 3), grid)\n\n# Filter the scalar potentials (use your filtering package) — this is what commutes with ∇.\nψ_filtered = your_filter(HD.streamfunction(result), grid, filter_scale)\nχ_filtered = your_filter(result.χ, grid, filter_scale)\n\n# Reconstruct velocity from the filtered potentials.","title":"Coarse-Graining Workflow"},{"category":"section","location":"architecture/#Architecture","page":"Architecture","text":"","title":"Architecture"},{"category":"section","location":"architecture/#Package-structure","page":"Architecture","text":"src/\n  HelmholtzDecomposition.jl   # module; qualified imports only, no exports\n  BoundaryConditions.jl       # Dirichlet, Neumann\n  Staggering.jl               # face/corner counts and indexing (Arakawa C-grid / MAC)\n  Operators.jl                # G, D = −G*, L = D G, curl, δ; face metrics; execution backend\n  Solvers.jl                  # AbstractPoissonSolver, CGSolver, AutoSolver, solver registry\n  Multigrid.jl                # geometric V-cycle preconditioner (Galerkin coarsening)\n  DualGrid.jl                 # the corner grid the rotation potential lives on\n  Decomposition.jl            # plan/workspace/result/batch and helmholtz_decompose[!]\n  Spectral.jl                 # Leray projector in mode space\nreference_flows/\n  reference_flows.jl          # analytic fields with known decompositions; NOT part of the library\next/\n  HelmholtzDecompositionAbstractFFTsExt.jl        # bounded + channel + periodic, any FFT backend\n  HelmholtzDecompositionFFTWExt.jl                # r2r fast path on the host\n  HelmholtzDecompositionFINUFFTExt.jl             # scattered Cartesian (binary NUFFT)\n  HelmholtzDecompositionNonuniformFFTsExt.jl      # scattered Cartesian (pure Julia, device-capable)\n  HelmholtzDecompositionFSHExt.jl                 # Clenshaw–Curtis sphere\n  HelmholtzDecompositionNUFSHTExt.jl              # arbitrary covering sphere\n  HelmholtzDecompositionKernelAbstractionsExt.jl  # device memory for the buffers\n  HelmholtzDecompositionOhMyThreadsExt.jl         # threaded batch\n  HelmholtzDecompositionDistributedExt.jl         # multiprocess batch\n  HelmholtzDecompositionMPIExt.jl                 # MPI batch\n  HelmholtzDecompositionCairoMakieExt.jl          # visualization\n\nGrids, geometries, masks, topology and the execution primitives come from FlowGeometries; the backend taxonomy from ComputationalBackends; the names for transform algorithms from SpectralBackends. This package defines none of them.","title":"Package structure"},{"category":"section","location":"architecture/#One-consistent-operator-family","page":"Architecture","text":"One gradient G is chosen — compact, cell centres to faces — and everything else is defined from it:\n\nD = −G*      (adjoint under the cell-measure inner product)\nL = D G      (so L = −G*G)\n\nL is then symmetric negative-semidefinite by construction, which is what makes conjugate gradients and multigrid applicable rather than merely plausible; div(u_div) = L χ = D u holds exactly; and u_div ⊥ u_rot discretely, so harmonic_fraction reports topology rather than disagreement between separately-chosen stencils.\n\nA masked face carries zero area. No-flux, the mask treatment and the preservation of adjointness are then the same statement, and no stencil reaches across a hole.","title":"One consistent operator family"},{"category":"section","location":"architecture/#Plan,-workspace,-result","page":"Architecture","text":" holds shared?\nHelmholtzPlan grid, Laplacian coefficients, face metrics, dual grids, solver choice one per grid, shared by a whole batch\nHelmholtzWorkspace face/corner buffers and solver state one per task\nHelmholtzResult / HelmholtzBatch outputs; the batch stores (dims…, N, B) contiguously per field / per batch\n\nSolver state is in the workspace, not the plan, because it holds buffers a solve writes through — the conjugate-gradient vectors, the multigrid level arrays, the transform scratch. On the plan, a threaded batch would have every task writing the same ones.","title":"Plan, workspace, result"},{"category":"section","location":"architecture/#Two-orthogonal-backend-axes","page":"Architecture","text":"Which transform — AbstractPoissonSolver. AutoSolver chooses on capability: geometry, node layout, mask, axis uniformity, boundary condition, and which extensions are loaded. It throws rather than downgrading, because a solver that silently solves a different boundary value problem returns a smooth, plausible, wrong field.\nWhere it runs — ComputationalBackends.AbstractExecutionBackend, passed to plan_helmholtz. Every hot loop is written once against FlowGeometries.Execution.run_indices, which resolves to a serial loop, threaded chunks, or a KernelAbstractions launch with no branching here.","title":"Two orthogonal backend axes"},{"category":"section","location":"architecture/#Solver-registration","page":"Architecture","text":"An extension declares which SpectralBackends algorithm it implements, with a priority:\n\nHelmholtzDecomposition.register_spectral_solver!(SpectralBackends.FFTSpectralBackend,\n                                                 CartesianBoundedSolver; priority = 10)\n\nTwo extensions may implement the same algorithm — FFTW.r2r and the AbstractFFTs even/odd extension are both the bounded FFT — so the priority is explicit and load order decides nothing.","title":"Solver registration"},{"category":"section","location":"architecture/#Type-hierarchy","page":"Architecture","text":"AbstractPoissonSolver\n├── AutoSolver\n├── CGSolver{T}                       # multigrid-preconditioned by default\n├── CartesianSpectralSolver           (ext: FFTW)          periodic rfft\n├── CartesianBoundedSolver            (ext: FFTW)          r2r, per-direction kind\n├── CartesianRealTransformSolver      (ext: AbstractFFTs)  bounded/channel/periodic, any backend\n├── CartesianNUFFTSolver{T}           (ext: FINUFFT)\n├── CartesianNonuniformFFTSolver{T}   (ext: NonuniformFFTs)\n├── SphericalSpectralSolver           (ext: FastSphericalHarmonics)\n└── SphericalNUSHTSolver{T}           (ext: NUFSHT)","title":"Type hierarchy"},{"category":"section","location":"architecture/#Import-style","page":"Architecture","text":"All imports are using X: X, called as X.method(). No top-level export, no const X = Submodule.X flattening. Enforced by Aqua.","title":"Import style"},{"category":"section","location":"#HelmholtzDecomposition.jl","page":"Home","text":"Helmholtz decomposition of 2D velocity fields into rotational and divergent components.","title":"HelmholtzDecomposition.jl"},{"category":"section","location":"#Overview","page":"Home","text":"This package decomposes a horizontal velocity field (u, v) into:\n\nRotational (non-divergent): urot, vrot derived from stream function ψ\nDivergent (irrotational): udiv, vdiv derived from velocity potential χ\n\nby solving the Poisson equations ∇²ψ = ζ and ∇²χ = δ.","title":"Overview"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url=\"https://github.com/jbphyswx/HelmholtzDecomposition.jl\")\n\nFor spectral solver performance, also install one of:\n\nPkg.add(\"FFTW\")                    # Cartesian periodic grids\nPkg.add(\"FastSphericalHarmonics\")  # Regular spherical grids\nPkg.add(url=\"https://github.com/jbphyswx/NUFSHT.jl\")  # Irregular spherical","title":"Installation"},{"category":"section","location":"#Usage","page":"Home","text":"See Examples for full worked examples, and Theory for mathematical background.","title":"Usage"},{"category":"section","location":"internals/#Internals","page":"Internals","text":"Not part of the interface and not subject to its stability. Documented because a reader following the operator construction or the solver dispatch will meet them, and because a manual that omits half of what a module defines is harder to trust than one that marks the boundary explicitly.","title":"Internals"},{"category":"section","location":"internals/#Spectral-result-types","page":"Internals","text":"The physical fields come back in a HelmholtzResult; these carry the spectral path's own intermediate form.","title":"Spectral result types"},{"category":"section","location":"internals/#Solver-plumbing","page":"Internals","text":"","title":"Solver plumbing"},{"category":"section","location":"internals/#Grid-walking","page":"Internals","text":"","title":"Grid walking"},{"category":"section","location":"internals/#Batch-and-backend-plumbing","page":"Internals","text":"","title":"Batch and backend plumbing"},{"category":"section","location":"internals/#Spectral-plumbing","page":"Internals","text":"","title":"Spectral plumbing"},{"category":"type","location":"internals/#HelmholtzDecomposition.AbstractSpectralHelmholtzResult","page":"Internals","text":"AbstractSpectralHelmholtzResult{T}\n\nAbstract supertype for spectral decomposition results.\n\n\n\n\n\n","title":"HelmholtzDecomposition.AbstractSpectralHelmholtzResult"},{"category":"type","location":"internals/#HelmholtzDecomposition.SpectralCartesianResult","page":"Internals","text":"SpectralCartesianResult{T,A}\n\nCartesian spectral decomposition result holding the component-last complex Fourier coefficients of the rotational, divergent and harmonic velocity ((kdims..., N)).\n\nu_harm carries the k = 0 mode. On a periodic domain the constant fields are exactly the harmonic subspace — H¹(Tᴺ) has dimension N, and a constant field is both curl-free and divergence-free — so the mean flow belongs to neither of the other two parts.\n\n\n\n\n\n","title":"HelmholtzDecomposition.SpectralCartesianResult"},{"category":"type","location":"internals/#HelmholtzDecomposition.SpectralSphericalResult","page":"Internals","text":"SpectralSphericalResult{T,V}\n\nSpherical spectral decomposition result holding the spherical-harmonic coefficients of the streamfunction ψ and velocity potential χ, up to degree lmax.\n\n\n\n\n\n","title":"HelmholtzDecomposition.SpectralSphericalResult"},{"category":"function","location":"internals/#HelmholtzDecomposition.build_cartesian_result","page":"Internals","text":"build_cartesian_result(grid, U, velocity_hat, ks, inverse) -> HelmholtzResult\n\nAssemble a physical HelmholtzResult from a component-last spectral velocity array. inverse(spectral_scalar) maps one spectral scalar field ((kdims...)) back to a physical one ((dims...)). Used by the regular-grid spectral extensions (FFTW).\n\nN + P + 2 inverse transforms are taken, not 2N + 2P + 2:\n\nu_rot is U − u_div − u_harm in physical space, so it costs nothing;\nu_harm is the k = 0 mode, i.e. a constant field, and is the component mean — no transform;\nthe rotation tensor W = ΔR is not materialised at all. It was P inverse transforms for a quantity recomputable from R, and no longer has a field on the result.\n\nOn this path rotation_potential holds cell-centred arrays, where the finite-difference path holds corner-staggered ones. The field is type-parameterised for exactly this reason, but the staggering does depend on which path produced the result.\n\n\n\n\n\n","title":"HelmholtzDecomposition.build_cartesian_result"},{"category":"type","location":"internals/#HelmholtzDecomposition.CGWorkspace","page":"Internals","text":"CGWorkspace{A}\n\nThe four vectors a conjugate-gradient iteration needs, held so the solve allocates nothing.\n\n\n\n\n\n","title":"HelmholtzDecomposition.CGWorkspace"},{"category":"type","location":"internals/#HelmholtzDecomposition.CGState","page":"Internals","text":"CGState{W,P}\n\nEverything CGSolver reuses across solves on one (grid, boundary): its vectors and its preconditioner.\n\nThe vectors belong here and not in a default argument. helmholtz_decompose! solves P + 1 right-hand sides and a batch multiplies that by the field count, so defaulting them allocates four grid arrays per solve for the lifetime of the program.\n\n\n\n\n\n","title":"HelmholtzDecomposition.CGState"},{"category":"function","location":"internals/#HelmholtzDecomposition._precondition!","page":"Internals","text":"_precondition!(z, r, grid, c, preconditioner)\n\nApply M⁻¹ to the residual: the Jacobi diagonal when there is no preconditioner, a V-cycle when there is (Multigrid.jl adds that method, where the type lives).\n\n\n\n\n\n","title":"HelmholtzDecomposition._precondition!"},{"category":"function","location":"internals/#HelmholtzDecomposition._detect_singular","page":"Internals","text":"_detect_singular(grid, c) -> Bool\n\nWhether the constants lie in L's null space, decided by applying L to the constant field and seeing whether anything comes back.\n\nAsking the boundary condition instead is not enough, and the dual grid is why: its boundary ring of corners has no closed loop of cells around it, so those corners are masked out and every face of the active region is closed — the operator there is singular even under Dirichlet, which a condition-based test reports as nonsingular. Conjugate gradients on a singular system with no null-space projection then drifts along the null space instead of converging, which showed up as a harmonic fraction of 1.7e+32.\n\nL·1 = 0 is exactly the property that matters and costs one operator application at plan time.\n\n\n\n\n\n","title":"HelmholtzDecomposition._detect_singular"},{"category":"function","location":"internals/#HelmholtzDecomposition._require_domain","page":"Internals","text":"_require_domain(solver, grid)\n\nThrow unless grid is a domain solver can solve on. Reads the mask, so it is O(N) and is called once where a solver is chosen — never from solve_poisson!, which a decomposition enters P + 1 times per field and once more per field of a batch.\n\n\n\n\n\n","title":"HelmholtzDecomposition._require_domain"},{"category":"function","location":"internals/#HelmholtzDecomposition._require_sampling","page":"Internals","text":"_require_sampling(solver, grid)\n\nExtra, solver-specific demand on the node layout — a transform defined on one node set and no other says so here. A hook rather than an override of _require_domain: overriding that would silently drop the mask, uniformity and topology checks it also performs.\n\n\n\n\n\n","title":"HelmholtzDecomposition._require_sampling"},{"category":"function","location":"internals/#HelmholtzDecomposition._spectral_algorithms","page":"Internals","text":"_spectral_algorithms(geometry_type) -> Tuple\n\nThe algorithms worth trying on a geometry, in preference order: the uniform transform first, then the non-uniform one, which subsumes it at greater cost. A candidate that does not apply refuses itself through its own capability check, so this order only decides between several that do.\n\n\n\n\n\n","title":"HelmholtzDecomposition._spectral_algorithms"},{"category":"function","location":"internals/#HelmholtzDecomposition._spectral_dispatch","page":"Internals","text":"_spectral_dispatch(u, grid; solver=AutoSolver(), kwargs...)\n\nResolve a spectral solver (extensions register them; AutoSolver picks the best available) and dispatch to the extension's _decompose_spectral(solver, geometry, u, grid; …). Dispatching on the solver type lets several spectral backends (FFTW + FINUFFT, FSH + NUFSHT) coexist for the same geometry without method clashes. The CUDA extension overrides this for CuArray inputs to take the CUFFT path directly.\n\n\n\n\n\n","title":"HelmholtzDecomposition._spectral_dispatch"},{"category":"function","location":"internals/#HelmholtzDecomposition.cell_above","page":"Internals","text":"cell_above(grid, F, d) -> CartesianIndex or nothing\n\nThe cell on the high side of face F along d; nothing at the outer edge of a bounded direction. On a periodic direction every face has both.\n\n\n\n\n\n","title":"HelmholtzDecomposition.cell_above"},{"category":"function","location":"internals/#HelmholtzDecomposition.cell_below","page":"Internals","text":"cell_below(grid, F, d) -> CartesianIndex or nothing\n\nThe cell on the low side of face F along d; nothing at the outer edge of a bounded direction.\n\n\n\n\n\n","title":"HelmholtzDecomposition.cell_below"},{"category":"function","location":"internals/#HelmholtzDecomposition.face_below","page":"Internals","text":"face_below(I, d) / face_above(grid, I, d) -> CartesianIndex\n\nThe two faces of cell I normal to d, as indices into face array d. On a periodic direction the face above the last cell wraps to face 1.\n\n\n\n\n\n","title":"HelmholtzDecomposition.face_below"},{"category":"function","location":"internals/#HelmholtzDecomposition.geometric_face_area","page":"Internals","text":"geometric_face_area(grid, F, d, T) -> T\n\nArea of face F normal to d: the product of the physical cell widths in every other direction, evaluated at the face. Purely geometric — whether flux crosses it is face_area's question.\n\n\n\n\n\n","title":"HelmholtzDecomposition.geometric_face_area"},{"category":"function","location":"internals/#HelmholtzDecomposition.copy_slice!","page":"Internals","text":"copy_slice!(dest, i, src, j) -> dest\n\nCopy slice j of one batch into slot i of another, fields and diagnostics alike.\n\n\n\n\n\n","title":"HelmholtzDecomposition.copy_slice!"},{"category":"function","location":"internals/#HelmholtzDecomposition._resolve_batch_backend","page":"Internals","text":"_resolve_batch_backend(backend, fields) -> AbstractExecutionBackend\n\nResolve Auto over the batch axis against real capability: more than one field, more than one thread, and the threading extension actually loaded. A concrete backend passes through and is then honoured or refused — never quietly replaced.\n\n\n\n\n\n","title":"HelmholtzDecomposition._resolve_batch_backend"},{"category":"function","location":"internals/#HelmholtzDecomposition._extension_loaded","page":"Internals","text":"_extension_loaded(name) -> Bool\n\nWhether one of this package's extensions is loaded. hasmethod cannot answer this — the dispatch stubs exist unconditionally, so it is always true.\n\n\n\n\n\n","title":"HelmholtzDecomposition._extension_loaded"},{"category":"function","location":"internals/#HelmholtzDecomposition._unsupported_backend_message","page":"Internals","text":"_unsupported_backend_message(backend, entry) -> String\n\nWhy entry cannot honour backend, and what to load so it can. Reaching this is always an error: a backend named explicitly is executed as named or refused, never downgraded.\n\n\n\n\n\n","title":"HelmholtzDecomposition._unsupported_backend_message"},{"category":"function","location":"internals/#HelmholtzDecomposition._grid_wavenumbers","page":"Internals","text":"_grid_wavenumbers(velocity_hat, grid) -> NTuple{N}\n\nReconstruct the per-axis angular wavenumber vectors for a component-last spectral array on a Cartesian grid. Axis 1 is treated as an rfft axis when its spectral length equals N₁÷2 + 1, otherwise as a full fft axis.\n\n\n\n\n\n","title":"HelmholtzDecomposition._grid_wavenumbers"},{"category":"function","location":"internals/#HelmholtzDecomposition._stack_components","page":"Internals","text":"_stack_components(grid, comps...) -> Array\n\nStack scalar component arrays into the component-last layout (dims..., M) the transforms take.\n\n\n\n\n\n","title":"HelmholtzDecomposition._stack_components"}]
}
